#ifndef VRT_SOCKET_SRC_PROCESS_H_
#define VRT_SOCKET_SRC_PROCESS_H_

namespace vrt::socket {

struct ProgramArguments;

void process(const ProgramArguments& args);

}  // namespace vrt::socket

#endif
